{
  "name": "lumera-vault",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "workspaces": [
    "contract",
    "ui"
  ],
  "overrides": {
    "@midnight-ntwrk/ledger-v8": "8.1.0",
    "@midnight-ntwrk/compact-js": "2.5.1",
    "@midnight-ntwrk/midnight-js-types": "4.1.1",
    "@midnight-ntwrk/midnight-js-contracts": "4.1.1",
    "@midnight-ntwrk/midnight-js-utils": "4.1.1",
    "@midnight-ntwrk/midnight-js-network-id": "4.1.1",
    "@midnight-ntwrk/midnight-js-fetch-zk-config-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-indexer-public-data-provider": "4.1.1"
  },
  "scripts": {
    "build:contract": "npm run compact --workspace contract && npm run build --workspace contract",
    "build:start": "npm run build --workspace ui && npm run start --workspace ui",
    "build": "npm run build:contract && npm run build --workspace ui",
    "start": "npm run start --workspace ui",
    "test": "npm run test --workspace contract",
    "typecheck": "npm run typecheck --workspace contract && npm run typecheck --workspace ui"
  }
}
